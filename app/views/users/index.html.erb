<table>
	<tr>
		<%= link_to"show user", user_path(user.id) %>
		<% end %>
	</tr>
</table>


<table>
	<tr>
		<td>first_name</td>
		<td>last_name</td>
		<td>email</td>
		<td>password</td>
		<td>phone</td>
		<td>group</td>
	</tr>
	
	<% @users.each do |user| %>
		<tr>
			<td><%= link_to user.first_name, user_path(user.id) %></td>
			<td><%= link_to user.last_name, user_path(user.id) %></td>
			<td><%= user.email %></td>
			<td><%= user.password %></td>
			<td><%= user.phone %></td>
			<td><%= user.group.group_name unless user.group.nil? %></td>

			<td><%=link_to "edit user", edit_user_path(user:id)%></td>
			<td><%= link_to "destroy user", user, confirm: "Are you sure you would like to delete your account?", method: :delete %></td>
				<!-- not sue if these go here or not, and why I can't get them to function properly in my paths -->
		</tr>
	<% end %>

</table>




